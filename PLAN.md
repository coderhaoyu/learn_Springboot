# Spring Boot 后端学习计划

## 总体目标

当前起点：

- 已完成基于内存数据的用户列表、详情、新增。
- 已理解基础 `Controller → Service` 调用。
- 尚未接入 MySQL、MyBatis。
- 尚未完成修改、删除、分页、异常处理、事务、安全与有效测试。

结业目标：

- 独立设计并完成一个“任务管理系统”后端。
- 能解释请求从 HTTP 到 MySQL，再返回 JSON 的完整流程。
- 能独立设计表、接口、分层、SQL、异常和测试。
- 能通过日志与调用栈定位常见问题。
- 能使用 Docker 启动应用和 MySQL。
- 暂不追求精通分布式、消息队列和高并发架构。

学习投入：每周 15–20 小时，其中约 70% 编码、20% 测试调试、10% 阅读总结。

基础语法与框架补课手册：[`docs/java-springboot-foundation.md`](docs/java-springboot-foundation.md)。

## 结业项目设计

采用单体 Spring Boot 项目，继续扩展当前仓库：

- Java 21、Spring Boot 4.1.0、Maven。
- Spring MVC、MyBatis XML、MySQL 8.x。
- JUnit 5、Spring Boot Test、MockMvc。
- Spring Security、JWT、BCrypt。
- Docker Compose 管理应用和 MySQL。
- 不使用 MyBatis-Plus、MapStruct、微服务或消息队列。

核心数据模型：

- `users`：用户、邮箱、密码摘要、创建与更新时间。
- `projects`：项目、负责人、名称、描述。
- `project_members`：项目成员及角色。
- `tasks`：任务、负责人、状态、优先级、截止时间、版本号。
- `task_comments`：作为最终独立考核模块。

核心接口：

- `/api/auth/register`、`/api/auth/login`
- `/api/users/me`
- `/api/projects`：新增、详情、分页、修改、删除
- `/api/projects/{id}/members`：添加、查询、移除成员
- `/api/projects/{projectId}/tasks`：新增与分页查询
- `/api/tasks/{id}`：详情、修改、删除
- `/api/tasks/{id}/status`：状态流转
- `/api/tasks/{taskId}/comments`：最终独立考核接口

统一约定：

- DTO 接收请求，VO 控制响应，不直接暴露 Entity。
- 分页响应使用 `PageResult<T>`。
- 普通响应使用 `ApiResponse<T>`。
- 参数错误返回 400，未登录返回 401，无权限返回 403，数据不存在返回 404，业务冲突返回 409。
- 项目负责人管理项目和成员；项目成员可以查看项目并管理任务。
- 创建项目时，同时写入负责人对应的成员记录，并用事务保证原子性。
- 任务通过版本号实现一次基础乐观锁练习。

## 周计划

详细执行清单：

- [第 1 周：完成真正的数据库 CRUD](docs/weekly/week-01-database-crud.md)
- [第 2 周：把 CRUD 做成规范接口](docs/weekly/week-02-standard-crud.md)
- [第 3 周：认证与权限](docs/weekly/week-03-auth-security.md)
- [第 4 周：项目与成员模块](docs/weekly/week-04-project-member.md)
- [第 5 周：任务模块与复杂查询](docs/weekly/week-05-task-query.md)
- [第 6 周：测试、SQL 与工程质量](docs/weekly/week-06-test-quality.md)
- [第 7 周：部署与项目收尾](docs/weekly/week-07-deployment.md)
- [最终验收：独立完成任务评论模块](docs/weekly/week-08-final-assessment.md)

### 第 1 周：完成真正的数据库 CRUD

学习内容：

- 补全当前内存版用户修改、删除，理解 PUT、DELETE 和 HTTP 状态码。
- 安装适用于 Apple Silicon 的 Docker Desktop，通过容器运行 MySQL。
- 设计 `users` 表，理解主键、自增、唯一约束和时间字段。
- 添加 MyBatis 与 MySQL 驱动。
- 编写 Mapper 接口和 XML SQL。
- 用数据库替换 `UserService` 中的内存列表。

本周交付：

- 用户新增、详情、列表、修改、删除全部访问 MySQL。
- 重启应用后数据仍然存在。
- 能口述请求参数如何绑定到 SQL，以及查询结果如何映射成 Java 对象。

### 第 2 周：把 CRUD 做成规范接口

学习内容：

- 创建用户新增、修改、查询 VO。
- 使用 Bean Validation 完成参数格式校验。
- 实现 `limit + offset` 分页和总数查询。
- 实现统一响应、业务异常和全局异常处理。
- 处理邮箱重复、用户不存在等业务场景。
- 学习数据库唯一约束与 Service 校验各自的作用。

本周交付：

- 用户模块具备完整 CRUD 和分页。
- 正常场景与 400、404、409 场景均能得到明确响应。
- Controller 只接收请求和调用 Service，业务规则全部位于 Service。
- 针对本周相关类完成定向测试，不运行无关的全项目检查。

### 第 3 周：认证与权限

学习内容：

- 理解认证和授权的区别。
- 注册时使用 BCrypt 保存密码摘要。
- 登录成功后签发 JWT。
- 使用 Spring Security 解析 `Authorization: Bearer <token>`。
- 从安全上下文中取得当前用户。
- 理解 401 与 403 的区别。

本周交付：

- 注册、登录、获取当前用户接口。
- 未登录不能访问业务接口。
- 密码不以明文保存或返回。
- 能解释一次 JWT 请求经过哪些过滤器和业务层。

### 第 4 周：项目与成员模块

学习内容：

- 设计一对多、多对多关系。
- 实现项目 CRUD 和成员管理。
- 创建项目时自动添加负责人为项目成员。
- 使用 `@Transactional` 保证多次数据库操作同时成功或回滚。
- 在 Service 中实现负责人和成员权限规则。

本周交付：

- 项目新增、分页、详情、修改、删除。
- 成员添加、列表、移除。
- 非成员无法访问项目，普通成员无法管理成员。
- 人为制造第二条 SQL 失败，验证创建项目事务会整体回滚。

### 第 5 周：任务模块与复杂查询

学习内容：

- 实现任务 CRUD。
- 按项目、状态、负责人、优先级组合筛选。
- 使用 MyBatis 动态 SQL。
- 实现任务状态流转。
- 使用 `version` 字段完成乐观锁，冲突时返回 409。
- 为常用查询条件设计联合索引。

本周交付：

- 任务新增、详情、分页、修改、删除和状态更新。
- 动态 SQL 只拼接实际传入的条件。
- 两次并发修改同一版本时，只允许一次成功。
- 能解释索引字段顺序为什么这样设计。

### 第 6 周：测试、SQL 与工程质量

学习内容：

- Mapper 测试：验证 SQL、参数绑定和结果映射。
- Service 测试：验证业务规则、权限和事务。
- MockMvc 测试：验证参数、状态码、JSON 和安全规则。
- 使用 `EXPLAIN` 检查分页与筛选查询。
- 引入数据库版本迁移，确保空数据库可自动初始化。
- 整理日志，学习从首个有效异常和 `Caused by` 定位问题。

必须覆盖：

- 注册及登录成功、邮箱重复、密码错误。
- 用户和项目不存在。
- 无权限访问项目。
- 创建项目事务回滚。
- 任务分页及组合筛选。
- 乐观锁冲突。
- 参数校验失败。

### 第 7 周：部署与项目收尾

学习内容：

- 区分开发、测试、生产配置。
- 数据库密码和 JWT 密钥通过环境变量传入。
- 编写应用镜像和 Docker Compose。
- 配置健康检查与基础运行日志。
- 整理 README、数据库关系图、接口说明和启动步骤。
- 使用 curl、Postman 或 Bruno 完成一次完整业务流程测试。

本周交付：

- 一条启动流程可以同时运行应用和 MySQL。
- 从注册、登录、创建项目、添加成员到创建并完成任务全部跑通。
- 仓库中不存在明文密码和密钥。
- 新开发者只阅读 README 即可启动项目。

### 最终验收

不参考现成实现，独立增加“任务评论”模块：

1. 自己设计表结构、外键和索引。
2. 自己设计新增、列表、删除接口。
3. 自己确定 Controller、Service、Mapper、DTO、VO。
4. 实现“项目成员可以评论，评论作者可以删除”的规则。
5. 编写相关测试并接入现有异常体系。
6. 更新接口文档和数据库关系图。

如果能在三天内独立完成该模块并定位开发中的错误，即达到“可以独立上手中小型后端项目”的阶段目标。

## 每周固定学习节奏

- 周一：复盘上周，画数据流和接口草图。
- 周二至周四：分层实现本周功能，每次只推进一个小闭环。
- 周五：集中处理异常场景和相关测试。
- 周六：完成端到端联调，约 4–5 小时。
- 周日：整理笔记、重写关键 SQL、口述请求流程，并补齐遗留问题。

每个功能都按照以下顺序完成：

```text
明确需求
→ 设计表结构
→ 设计 API
→ 划分各层职责
→ 自己编写
→ 接口验证
→ 异常场景
→ 针对性测试
→ 总结请求与数据流
```

遇到问题时记录：

- 现象与请求参数。
- HTTP 状态码。
- 第一个有效异常。
- 最底层 `Caused by`。
- 涉及的 Controller、Service、Mapper 和 SQL。
- 最终原因及验证方法。

## 验收标准

必须能够独立完成：

- 根据需求设计表、外键、唯一约束和基础索引。
- 设计符合 REST 风格的接口。
- 正确划分 Controller、Service、Mapper、DTO、VO。
- 编写 MyBatis 参数绑定、结果映射、动态 SQL 和分页。
- 处理参数错误、数据不存在、业务冲突和无权限。
- 使用事务保证多表操作一致性。
- 编写关键 Mapper、Service 和接口测试。
- 阅读 Spring 启动异常、调用栈和 SQL 错误。
- 使用 Docker 启动并交付项目。
- 完整解释 `HTTP → Controller → Service → Mapper → MySQL → JSON`。

## 后续进阶路线

第一阶段，4 周：

- MySQL 执行计划、覆盖索引、回表、慢查询。
- 事务隔离级别、脏读、不可重复读、幻读。
- 乐观锁、悲观锁、死锁和并发更新。
- Java 集合、泛型、异常、Stream 和常用并发工具。

第二阶段，4 周：

- 更完整的单元测试、集成测试和测试数据隔离。
- 日志、Actuator、健康检查和基础监控。
- CI/CD、配置管理和数据库迁移。
- 文件上传、定时任务和异步任务。

第三阶段，4–6 周：

- Redis 缓存、缓存一致性和缓存失效。
- 接口幂等、限流和防重复提交。
- 消息队列的基本使用与可靠性。
- 只有单体项目足够熟练后，再开始学习微服务与分布式系统。

## 明确取舍

- 以“能够独立交付项目”为目标，不追求所有原理的深度。
- 暂不学习 MyBatis-Plus、复杂设计模式、Redis、消息队列和微服务。
- 不另外开发完整前端，以接口测试和现有前端能力完成联调。
- 学习过程中由你先设计和编写，Agent 主要负责分步提示、代码评审和错误分析。
