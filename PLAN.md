# “我们的冒险”全栈项目与后端学习计划

## 1. 总体目标

本项目从当前的用户 CRUD 基础继续扩展，最终做成一个私密的情侣共同挑战与时光记录应用，暂定名称为“我们的冒险”。

产品完整设计见：[产品设计：我们的冒险](docs/couple-challenge-design.md)。

基础补课手册见：[Java 与 Spring Boot 后端学习手册](docs/java-springboot-foundation/README.md)。

学习顺序明确为：

```text
先设计完整产品
    ↓
先完成后端核心闭环
    ↓
用接口工具验证后端
    ↓
再开发前端页面
    ↓
完成前后端联调
```

当前阶段不直接开发前端。前端模块、页面和接口依赖已经先完成设计，等后端核心接口稳定后再进入前端阶段。

## 2. 当前起点

仓库目前已经具备用户模块的后端基础：

- Spring Boot 启动类；
- 用户 Entity、DTO、VO；
- UserController、UserService、UserMapper；
- MyBatis XML；
- MySQL 数据源配置和 `users` 建表脚本；
- 统一响应、分页结果、业务异常和全局异常处理的基础结构。

尚未完成或需要继续学习的内容：

- 注册和登录；
- Spring Security 与 JWT；
- 情侣绑定；
- 挑战、参与和每日打卡；
- 积分、成就和并发一致性；
- 时光记录和纪念日；
- 定向测试、部署和后端交付；
- 前端工程和前后端联调。

## 3. 项目定位

“我们的冒险”不是聊天软件，也不是公开社交平台。第一阶段只做两个人的私密共同空间：

```text
注册登录
    ↓
绑定伴侣
    ↓
加入共同挑战
    ↓
每日打卡
    ↓
查看进度、积分和成就
    ↓
记录共同生活和纪念日
```

### 第一版必须完成

- 用户注册、登录和当前用户；
- 邀请码绑定情侣关系；
- 系统预置挑战；
- 情侣加入和退出挑战；
- 每日共同打卡；
- 进度、连续天数和积分查询；
- 私密数据访问权限；
- 防重复打卡、一次性邀请码和关键事务。

### 后续扩展

- 时光记录时间线；
- 纪念日和倒计时；
- 固定规则成就；
- 首页汇总数据；
- 图片记录、评论和提醒。

### 当前明确不做

- 实时聊天、语音和视频；
- 推送通知；
- 图片和视频存储；
- 地图轨迹；
- 支付、商城和虚拟货币；
- AI 功能；
- 多人群组、微服务、消息队列和分布式锁。

## 4. 技术栈

### 当前后端阶段

- Java 21；
- Spring Boot 4.1.0；
- Spring MVC；
- MyBatis XML；
- MySQL 8.x；
- Maven；
- JUnit 5；
- Spring Boot Test；
- Spring Security、JWT、BCrypt；
- Docker（后期交付）。

### 后续前端阶段

前端使用：

- React；
- TypeScript；
- Vite；
- React Router；
- Zustand；
- Axios。

前端工程骨架可以先建立；业务页面和接口联调仍在后端接口稳定后进行。

## 5. 后端模块总览

| 模块 | 主要职责 | 阶段 |
|---|---|---|
| `common` | 统一响应、分页、异常、配置 | 基础 |
| `auth` | 注册、登录、Token | 核心 |
| `user` | 当前用户资料、认证查询 | 核心 |
| `couple` | 邀请码、情侣绑定、关系权限 | 核心 |
| `challenge` | 挑战目录、详情、参与 | 核心 |
| `checkin` | 每日打卡、进度、积分 | 核心 |
| `moment` | 共同生活记录 | 扩展 |
| `anniversary` | 纪念日和倒计时数据 | 扩展 |
| `achievement` | 成就定义和解锁记录 | 结业练习 |
| `dashboard` | 首页汇总和统计 | 扩展 |

模块、数据模型、权限、API 和前端页面的完整说明见产品设计文档，不在学习计划中重复展开。

## 6. 后端接口范围

后端学习阶段围绕以下接口组展开：

### 认证和用户

- `POST /api/auth/register`；
- `POST /api/auth/login`；
- `GET /api/users/me`；
- `PATCH /api/users/me`。

### 情侣关系

- `POST /api/couples/invitations`；
- `POST /api/couples/bind`；
- `GET /api/couples/me`。

### 挑战和参与

- `GET /api/challenges`；
- `GET /api/challenges/{id}`；
- `POST /api/challenges/{id}/join`；
- `GET /api/couples/me/challenges`；
- `GET /api/couples/me/challenges/{id}`；
- `POST /api/couples/me/challenges/{id}/leave`。

### 打卡和进度

- `POST /api/couples/me/challenges/{id}/check-ins`；
- `GET /api/couples/me/challenges/{id}/check-ins`；
- `GET /api/couples/me/challenges/{id}/progress`。

### 扩展接口

- `/api/moments`：共同生活记录 CRUD；
- `/api/anniversaries`：纪念日 CRUD；
- `/api/dashboard/summary`：首页汇总；
- `/api/achievements`：成就查询。

## 7. 后端八周学习计划

每周建议投入 15–20 小时。前 7 周以编码、接口验证和相关测试为主，第 8 周进行质量收尾和独立验收。

详细任务：

- [第 1 周：用户数据库 CRUD](docs/weekly/week-01-database-crud.md)
- [第 2 周：规范接口与异常](docs/weekly/week-02-standard-crud.md)
- [第 3 周：认证与安全](docs/weekly/week-03-auth-security.md)
- [第 4 周：情侣绑定与关系权限](docs/weekly/week-04-couple-binding.md)
- [第 5 周：挑战与参与关系](docs/weekly/week-05-challenge-participation.md)
- [第 6 周：打卡、进度与并发一致性](docs/weekly/week-06-checkin-concurrency.md)
- [第 7 周：时光记录、纪念日与汇总](docs/weekly/week-07-moment-anniversary.md)
- [第 8 周：测试、交付与独立成就模块](docs/weekly/week-08-final-assessment.md)

### 第 1 周：用户数据库 CRUD

目标：把当前用户模块作为稳定的后端基础，完整理解：

```text
HTTP → Controller → Service → Mapper → MySQL → JSON
```

完成用户新增、详情、分页、修改和删除，并确认数据库约束、MyBatis 参数绑定、结果映射和受影响行数。

### 第 2 周：规范接口与异常

目标：完善用户接口边界：

- DTO、VO 和 Entity 分工；
- 参数校验；
- 分页参数和响应；
- 统一响应；
- 400、404、409 和 500；
- Service 业务校验与数据库约束；
- 只运行相关文件和相关测试的定向检查。

### 第 3 周：认证与安全

目标：完成注册、登录和当前用户，理解：

- BCrypt 密码摘要；
- JWT 签发和验证；
- Security Filter Chain；
- SecurityContext；
- 401 和 403；
- 密码、Token 和敏感字段的边界。

### 第 4 周：情侣绑定与关系权限

目标：完成从两个独立用户到一段情侣关系的业务闭环：

- 创建一次性绑定邀请；
- 验证邀请码、有效期和使用状态；
- 创建情侣关系；
- 查询当前伴侣；
- 防止自己绑定自己和重复绑定；
- 使用事务保证绑定过程的一致性；
- 将关系权限放在 Service 判断。

### 第 5 周：挑战与参与关系

目标：从单表 CRUD 进入业务关联：

- 查询系统预置挑战；
- 查询挑战详情；
- 情侣加入挑战；
- 查询参加中的挑战；
- 退出尚未开始的挑战；
- 处理重复参加和非法状态；
- 设计外键、唯一约束和查询索引。

### 第 6 周：打卡、进度与并发一致性

目标：完成项目最有特色的业务模块：

- 每日共同打卡；
- 按挑战查询打卡时间线；
- 计算完成天数和连续天数；
- 积分更新；
- 防止同一天重复打卡；
- 讨论重复请求、丢失更新和成就重复解锁；
- 使用唯一约束、条件更新、事务或乐观锁解决具体问题；
- 用定向并发测试验证关键规则。

### 第 7 周：时光记录、纪念日与汇总

目标：补齐产品的记录和展示数据：

- 时光记录文本 CRUD；
- 时间线分页和日期筛选；
- 记录作者权限；
- 纪念日 CRUD；
- 周期性纪念日查询；
- 计算即将到来的纪念日；
- 汇总当前情侣、挑战、打卡和纪念日数据；
- 练习多表查询和聚合 SQL。

### 第 8 周：测试、交付与独立成就模块

目标：对后端整体质量负责，并独立完成一个小型模块：

- Mapper、Service、MockMvc 定向测试；
- 空数据库初始化；
- SQL 和索引检查；
- 日志和异常定位；
- 开发、测试配置分离；
- Docker Compose 启动应用和 MySQL；
- 独立完成成就定义和情侣成就解锁查询；
- 更新 README、接口说明和数据关系图。

## 8. 前端后置计划

前端不计入当前八周后端结业标准。后端完成后再按以下顺序推进：

### 前端阶段一：工程和认证

- 初始化 `frontend/` 前端工程骨架；
- 配置路由、请求层和状态管理；
- 完成注册、登录、退出；
- 处理 Token 和路由守卫。

### 前端阶段二：情侣和挑战

- 情侣绑定引导；
- 首页摘要；
- 挑战列表和详情；
- 加入挑战；
- 当前挑战进度。

### 前端阶段三：打卡和记录

- 今日打卡；
- 打卡日历；
- 连续天数和积分；
- 时光记录时间线；
- 纪念日倒计时。

### 前端阶段四：体验优化

- 成就展示；
- 加载、空状态和错误状态；
- 移动端布局；
- 图片记录（如果后端已准备好）；
- 前后端联调和发布。

## 9. 每周固定学习节奏

- 周一：复盘上周，阅读相关章节，画数据流和表关系；
- 周二至周四：每次只推进一个后端小闭环；
- 周五：补充异常场景和相关测试；
- 周六：使用接口工具完成端到端验证；
- 周日：整理笔记、重写关键 SQL、口述请求链路。

每个功能都遵循：

```text
明确需求
→ 设计表结构
→ 设计 API
→ 划分 Controller / Service / Mapper 职责
→ 自己编写
→ 接口验证
→ 异常场景
→ 定向测试
→ 总结请求与数据流
```

## 10. 后端验收标准

必须能够独立完成并解释：

- 用户注册、登录和当前用户查询；
- 两个用户通过邀请码建立情侣关系；
- 情侣加入挑战并完成每日打卡；
- 重复打卡、无效邀请码和越权访问被正确拒绝；
- 进度、连续天数和积分与数据库记录一致；
- DTO、Entity、VO 的边界清晰；
- Controller、Service、Mapper 没有越层职责；
- MyBatis 参数绑定、结果映射、动态 SQL 和分页正确；
- 事务、唯一约束和并发更新的作用可以区分；
- 关键成功和失败场景有定向测试；
- 可以通过日志和 `Caused by` 定位常见错误；
- 可以使用 Docker 启动应用和 MySQL；
- 能完整说明 `HTTP → Controller → Service → Mapper → MySQL → JSON`。

## 11. 后续进阶路线

后端主线完成后，再按需要学习：

1. 图片上传、对象存储和资源权限；
2. 定时任务和纪念日提醒；
3. Redis 缓存和缓存一致性；
4. MongoDB 文档数据库（可选）；
5. 接口幂等、限流和防重复提交；
6. 消息队列；
7. 多实例部署和分布式锁；
8. 只有单体项目稳定后，再学习微服务。

### Redis 专项阶段（后置学习，不纳入当前八周计划）

Redis 放在后端主线和前端第一轮联调完成之后，作为独立的基础设施练习，不影响当前 CRUD、认证、业务模块和测试的推进顺序。

建议按以下顺序学习：

1. 给用户详情查询增加 Cache Aside 缓存；
2. 理解缓存命中、缓存未命中和 TTL；
3. 在用户修改、删除后处理缓存失效；
4. 再练习邮箱查询缓存、访问次数统计和登录会话；
5. 最后再学习缓存穿透、缓存击穿、限流和分布式锁等问题。

第一阶段只使用 Redis 辅助 MySQL，不用 Redis 替代关系型数据库，也不提前引入复杂的缓存抽象。

### MongoDB 专项阶段（可选，晚于 Redis）

MongoDB 不作为当前项目核心业务数据库。用户、情侣关系、挑战参与、每日打卡和积分等数据具有明显的关系、约束和事务需求，继续使用 MySQL 更适合当前学习目标。

完成 MySQL 主线和 Redis 基础后，再用一个边界清晰的扩展场景练习 MongoDB，例如：

1. 将时光记录设计成文档集合，练习文档结构和嵌套数据；
2. 设计活动日志或操作记录，练习按时间查询和文档索引；
3. 对比 MySQL 表和 MongoDB 文档在关联、更新、分页和索引上的差异；
4. 理解什么场景适合文档数据库，以及为什么不能为了使用 MongoDB 而迁移所有核心表。

MongoDB 是数据库模型对比和扩展练习，不纳入当前后端八周验收标准，也不与 Redis 同时提前引入项目。

## 12. 学习取舍

- 先确保每个模块都有一条可解释、可验证的后端闭环；
- 不为了目录形式提前重构现有用户代码；
- 不使用 MyBatis-Plus 掩盖 SQL；
- 不在开发者尝试前直接生成完整业务代码；
- 不把前端页面当作后端逻辑的替代品；
- 前端后做，但后端 API 从一开始就按前端可消费的 DTO/VO 设计；
- 所有本地代码检查只针对本次修改的文件，不做项目范围的无关检查。
