<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.ouradventure.mapper.CoupleMapper">
    <insert id="addInvitationCode">
        INSERT INTO couple_invitations(inviter_id, code, expires_at, status)
        VALUES (#{inviterId}, #{code}, #{expiresAt}, #{status})
    </insert>
    <select id="findActiveByUserId" resultType="com.example.ouradventure.entity.Couple">
        SELECT id
        from couples
        WHERE (#{userId} = user_a_id OR #{userId} = user_b_id)
          AND status = 'ACTIVE'
    </select>
</mapper>